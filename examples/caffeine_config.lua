-- configuration for my caffeine.lua preferences
-- luacheck: globals hs

local caffeine_config = {
  disabled = true,  -- not needed so much any more
  keys = {
    caffeine = "c",
  },
}
return caffeine_config
