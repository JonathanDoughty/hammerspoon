# Example configuration

Configuration files here are the baseline for controlling aspects of the
similarly named, minus `-config`, `.lua` files in the parent directory. To
keep your configuration changes separate from future updates make a copy in
the corresponding `../configs` directory and modify those as desired.
